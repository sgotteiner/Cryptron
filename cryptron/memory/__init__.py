"""Memory, layer 2: the finds vault + the retrieval contract (memory_design.md §4-6).

finds.py  — the vault: one markdown file per find, envelope + lifecycle.
recall.py — the three access paths: scope-match, vector recall, spine/link-walk.
embed.py  — one embedding model, one space.
"""
